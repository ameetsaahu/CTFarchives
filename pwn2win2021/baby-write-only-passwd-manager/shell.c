#include <stdio.h>
#include <stdlib.h>

int main()
{
	system("cat /proc/cpuinfo");
	//system("ps -aux | grep server");
	//system("ls | grep \"flag\" && sleep 3");
	//printf("Hello blablablaaadssd!\n");
	return 0;
}